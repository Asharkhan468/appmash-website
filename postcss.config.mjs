const config = {
  plugins: {
    "@tailwindcss/postcss": {
      // Optional: Add Tailwind v4 specific options here
    },
    "autoprefixer": {} // Important for production builds
  }
};

export default config;